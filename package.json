{
  "name": "textlint-rule-ignore-node-types",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/textlint/textlint-rule-ignore-node-types.git"
  },
  "author": "azu",
  "email": "azuciao@gmail.com",
  "homepage": "https://github.com/textlint/textlint-rule-ignore-node-types",
  "license": "MIT",
  "files": [
    "src/",
    "lib/"
  ],
  "bugs": {
    "url": "https://github.com/textlint/textlint-rule-ignore-node-types/issues"
  },
  "version": "0.1.0",
  "description": "texlint rule that ignore reported error by user defeined node's type.",
  "main": "lib/textlint-rule-ignore-node-types.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build",
    "test": "mocha"
  },
  "keywords": [
    "textlint",
    "ignore",
    "disable"
  ],
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-jsdoc-to-assert": "^1.0.1",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.7.2",
    "mocha": "^2.4.5",
    "power-assert": "^1.3.1",
    "textlint": "^6.5.0",
    "textlint-rule-report-node-types": "^1.0.1"
  },
  "peerDependencies": {
    "textlint": ">= 6.5.0"
  }
}
